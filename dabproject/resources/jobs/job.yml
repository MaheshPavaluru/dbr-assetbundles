resources:
  jobs:
    ingestion_job:
      name: ingestion_job
      tasks:
        - task_key: ingestion
          notebook_task:
            notebook_path: /Workspace/Users/p.mahesh060@gmail.com/dbr-assetbundles/dabproject/src/notebooks/ingestion
            base_parameters:
              catalog_name: ${var.catalog_name}
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZED